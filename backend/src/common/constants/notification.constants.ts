export enum NotificationType {
  DEPOSITE_NOTIFICATION = 'deposite_notification',
  COMMENT_NOTIFICATION = 'comment_notification',
  STORY_NOTIFICATION = 'story_notification',
}

export enum NotificationStatus {
  CREATED = 'created',
  SENT = 'sent',
  VIEWED = 'viewed',
}
